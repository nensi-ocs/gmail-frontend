NEW_DIR="/var/www/html/new"
OLD_DIR="/var/www/html/old"

# if [ -d "$NEW_DIR" ]; then
#     if [ -d "$OLD_DIR" ]; then
#         sudo rm -rf "$OLD_DIR"
#     fi

#     sudo cp -r "$NEW_DIR" "$OLD_DIR"
# fi
echo "code deploy app backup script"